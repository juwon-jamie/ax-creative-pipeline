# Vendor Adapters

Concrete vendor implementations will live here after the user approves model choices and cost limits.

Rules:

- Read API keys from `.env`.
- Do not hard-code vendor names outside configuration.
- Do not commit generated secrets, prompts from private work, or real client assets.
